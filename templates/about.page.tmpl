{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="class col">
                <h1>This is the About page</h1>
                <p>This is some text About</p>
                <p>This is some text About</p>
                <p>This came from the template {{index .StringMap "test"}}</p>
                <p>
                    {{if ne (index .StringMap "remote_ip") ""}}
                     Your remote IP address is {{index .StringMap "remote_ip"}}
                    {{else}}
                    I don't know you ip address yet. Visit the <a href="/">Homepage</a> so I can set it.
                    {{end}}
                    
                </p>
            </div>
        </div>      
    </div>
{{end}}